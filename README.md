# JIT Modules

Public modules for the JIT platform.

Modules are developed independently from JIT-Node and interact with the platform only through the public JIT Module contract / SDK.

Initial modules:
- Probe — minimal contract and runtime validation module
- Chat CLI — replicated command-line chat module
